import styles from '../../styles/Home.module.css';
import Image from 'next/image';

const Footer = () => {
  return <footer className={styles.footer}>Copyright Covaya</footer>;
};

export default Footer;
